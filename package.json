{
  "name": "finpulse",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node scripts/dedup.test.js && node scripts/telegram.test.js && node scripts/guides.test.js && node build.js && node build.test.js",
    "generate": "node scripts/generate.js"
  }
}
