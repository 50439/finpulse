# Сборка сайта. Генерируется build.js и деплоится из GitHub Actions
# через upload-pages-artifact — в репозитории ей делать нечего.
# Без этого файла 572 сгенерированных файла (14 МБ) попадали бы
# в каждый авто-коммит бота и засоряли историю.
dist/

node_modules/
.DS_Store

# Готовые ролики и промежуточные кадры — забираются артефактом прогона,
# в репозитории им делать нечего.
out/
